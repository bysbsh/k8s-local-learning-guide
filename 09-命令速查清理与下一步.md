# 第 9 篇：命令速查、清理与下一步

## 本篇概述

- **为什么要学**：Kubernetes 命令多，不需要全部背诵，但必须能快速找到正确命令；清理操作还会影响 Pod、存储甚至整个集群，理解删除范围可以避免误删数据或被 Argo CD 自动重建。
- **包含什么**：查询、日志、发布、网络、存储、Helm、Argo CD、镜像和清理命令。
- **开始前需要**：没有强制前置条件，可在学习过程中随时查阅。
- **完成标志**：知道 `Ctrl+C`、删除业务资源、删除 Application 和删除 Kind 集群的影响范围。

## 1. 集群与命名空间

```bash
kubectl config current-context
kubectl get nodes
kubectl get namespaces
kubectl get all -n k8s-study
kubectl get pods -A
```

## 2. Deployment 和 Pod

```bash
kubectl get deployments,pods -n k8s-study
kubectl describe deployment my-app -n k8s-study
kubectl describe pod POD_NAME -n k8s-study
kubectl logs POD_NAME -n k8s-study
kubectl logs deployment/my-app -n k8s-study
kubectl get events -n k8s-study --sort-by=.lastTimestamp
```

## 3. 发布和回滚

```bash
kubectl rollout status deployment/my-app -n k8s-study
kubectl rollout history deployment/my-app -n k8s-study
kubectl rollout undo deployment/my-app -n k8s-study
```

## 4. Service 和 Ingress

```bash
kubectl get services,endpointslices -n k8s-study
kubectl get ingress -n k8s-study
kubectl port-forward service/web-service -n k8s-study 8080:80
kubectl port-forward service/traefik -n traefik 8090:80
```

## 5. 配置和存储

```bash
kubectl get configmaps,secrets -n k8s-study
kubectl get storageclass
kubectl get pvc -n k8s-study
kubectl get pv
```

## 6. Helm

```bash
helm list -A
helm history traefik -n traefik
helm status traefik -n traefik
helm uninstall traefik -n traefik
```

## 7. Argo CD

```bash
kubectl get application k8s-study -n argocd
kubectl get application k8s-study -n argocd --watch
kubectl get pods -n argocd
kubectl logs statefulset/argocd-application-controller -n argocd
```

主动刷新 Git：

```bash
kubectl annotate application k8s-study \
  -n argocd \
  argocd.argoproj.io/refresh=hard \
  --overwrite
```

## 8. Docker 和 Kind 镜像

```bash
docker images study-web
docker image inspect study-web:v2
kind load docker-image study-web:v2 --name study
docker exec study-control-plane crictl images | grep study-web
```

## 9. 停止 port-forward

在运行 port-forward 的终端按：

```text
Ctrl+C
```

只停止 Mac 到集群的临时入口，不删除 Kubernetes 资源。

## 10. 删除由 Argo CD 管理的资源

如果资源仍在 Git 中，而且 `selfHeal: true`，直接执行 `kubectl delete` 后可能被 Argo CD 重新创建。

正确流程：

```text
从 Git 删除 YAML
  ↓
git commit / git push
  ↓
如果 prune=true，Argo CD 自动删除
如果 prune=false，手动删除集群资源
```

手动删除示例：

```bash
kubectl delete deployment my-app -n k8s-study
kubectl delete service my-app -n k8s-study
kubectl delete ingress my-app -n k8s-study
```

## 11. 停止 Argo CD 管理

```bash
kubectl delete application k8s-study -n argocd
```

删除 Application 后，Argo CD 不再协调这套资源。当前配置没有启用自动 prune，业务资源通常会保留。

## 12. 删除整个学习集群

```bash
kind delete cluster --name study
```

这会删除：

- Kind 节点。
- 所有 Kubernetes 资源。
- Argo CD。
- Helm Release。
- PVC/PV 数据。
- 加载到 Kind 节点的镜像副本。

不会删除：

- `~/k8s-study` Git 仓库。
- Mac Docker 中构建的镜像。
- kubectl、Kind、Helm 软件。

删除 Mac Docker 镜像：

```bash
docker image rm study-web:v1 study-web:v2
```

## 13. 当前达到的水平

已经掌握：

- Pod、Deployment、Service、Ingress。
- ConfigMap、Secret、Namespace。
- 健康检查与资源限制。
- Helm 安装第三方组件。
- Argo CD 自动同步、selfHeal 和 prune。
- PVC/PV 持久化关系。
- Docker 镜像构建、Kind 加载和滚动更新。

这已经足够独立部署普通 Web 服务。

## 14. 下一阶段学习路线

不要继续堆组件，优先部署一个真实项目：

```text
Java/Spring Boot 源码
  ↓
构建 JAR
  ↓
编写 Dockerfile
  ↓
推送 Docker Hub / GHCR / 私有仓库
  ↓
Deployment + Service + Ingress
  ↓
Git + Argo CD
```

之后按实际需求学习：

1. CI 自动构建镜像并更新版本。
2. 正式域名和 HTTPS。
3. StatefulSet、Job、CronJob、HPA。
4. RBAC 和 NetworkPolicy。
5. Prometheus/Grafana、日志和告警。
6. 备份、升级与多节点高可用。

Rancher 是集群管理平台，不是 Kubernetes 的前置知识。先理解底层对象，再使用 Rancher 会更容易。

## 15. 最后的核心记忆

```text
Pod 会变，Service 提供稳定入口。
容器会重建，PVC 保存持久数据。
Ingress 只描述路由，Controller 才真正转发流量。
Helm 管理应用包，Argo CD 管理 Git 与集群的一致性。
生产配置进入 Git，Git 是期望状态的真实来源。
```

[上一篇：常见故障排查手册](./08-常见故障排查手册.md) | [下一篇：Kubernetes 术语索引](./10-术语索引.md) | [返回目录](./README.md)
