# 第 8 篇：常见故障排查手册

## 本篇概述

- **为什么要学**：Kubernetes 的问题可能发生在 YAML、调度、镜像、容器、网络或存储任意一层。只反复重试通常无效；掌握固定排查顺序，才能根据状态、Events 和日志定位真正原因。
- **包含什么**：今天实际遇到的 YAML、镜像、Helm、Argo CD、PVC、watch 和 zsh 报错。
- **如何使用**：复制终端中的原始错误关键词，在本篇搜索对应标题，再按给出的诊断顺序执行。
- **完成标志**：能先使用 `get`、`describe`、`logs` 和 `events` 定位问题层级。

## 1. 通用排查顺序

遇到资源异常时，按下面顺序检查：

```bash
# 1. 当前资源状态
kubectl get pods -n k8s-study -o wide

# 2. 资源详细信息和 Events
kubectl describe pod POD_NAME -n k8s-study

# 3. 容器日志
kubectl logs POD_NAME -n k8s-study

# 4. 命名空间事件
kubectl get events -n k8s-study --sort-by=.lastTimestamp

# 5. Deployment 发布状态
kubectl rollout status deployment/DEPLOYMENT_NAME -n k8s-study
```

先读 Events，再猜原因。

## 2. `apiVersion not set`

报错：

```text
error validating data: apiVersion not set
```

原因：YAML 缺少顶层 `apiVersion`，或缩进/多文档分隔符错误。

每个资源至少包含：

```yaml
apiVersion: ...
kind: ...
metadata:
  name: ...
```

不要使用 `--validate=false` 掩盖 YAML 结构错误。

## 3. 缺少 last-applied annotation

警告：

```text
missing the kubectl.kubernetes.io/last-applied-configuration annotation
```

资源最初不是用 `kubectl apply` 创建。kubectl 通常会自动补上注解，本次 apply 仍可成功。

长期方案：从一开始使用 YAML 和 `kubectl apply`，或直接让 Argo CD 管理。

## 4. YAML 缩进错误

典型问题：把 `image`、探针、端口和资源限制错误缩进到 `volumes` 项目中，导致：

```text
cannot restore struct from: string
```

正确层级：

```text
spec.template.spec
├── containers
│   └── image / ports / probes / resources / volumeMounts
└── volumes
    └── configMap / secret / persistentVolumeClaim
```

先检查：

```bash
kubectl apply --dry-run=client -f web.yaml
```

## 5. `mountPath must be unique`

报错：

```text
volumeMounts[1].mountPath: Invalid value: "/usr/share/nginx/html": must be unique
```

同一个容器把两个 Volume 挂载到了相同目录。删除重复挂载，或者使用不同的 `mountPath`。

## 6. `ImagePullBackOff`

常见原因：

- 镜像名称或标签错误。
- 镜像仓库不可访问。
- 私有仓库缺少认证。
- Kind 节点中没有本地镜像。

诊断：

```bash
kubectl describe pod POD_NAME -n k8s-study
kubectl get events -n k8s-study --sort-by=.lastTimestamp
```

本地镜像方案：

```bash
kind load docker-image study-web:v2 --name study
```

并使用：

```yaml
imagePullPolicy: Never
```

## 7. `ContainerCreating` 或节点 `NotReady`

刚创建节点、Pod 或拉取镜像时短暂出现这些状态通常正常：

```bash
kubectl get nodes --watch
kubectl get pods -n k8s-study --watch
```

如果长时间不变化，再使用 `describe` 和 `events`。

## 8. 临时 curl Pod 的 attach 警告

```text
couldn't attach to pod/test-client, falling back to streaming logs
```

如果同时返回 `HTTP/1.1 200 OK`，请求已经成功。临时容器执行太快，kubectl 来不及 attach，于是改为读取日志。

## 9. Argo CD CRD annotation 太长

报错：

```text
metadata.annotations: Too long: may not be more than 262144 bytes
```

解决：

```bash
kubectl apply --server-side --force-conflicts \
  -n argocd \
  -f https://raw.githubusercontent.com/argoproj/argo-cd/stable/manifests/install.yaml
```

## 10. Argo CD 显示 Synced 但没有最新资源

原因：Argo CD 与缓存中的旧 Git 版本一致，但还没轮询到最新提交。

对比版本：

```bash
git rev-parse --short HEAD
git ls-remote origin refs/heads/main
kubectl get application k8s-study -n argocd \
  -o jsonpath='{.status.sync.revision}{"\n"}'
```

主动刷新：

```bash
kubectl annotate application k8s-study \
  -n argocd \
  argocd.argoproj.io/refresh=hard \
  --overwrite
```

## 11. Argo CD 页面 `removeChild` 错误

如果 Kubernetes 资源和 Argo CD API 正常，而页面出现：

```text
NotFoundError: Failed to execute 'removeChild' on 'Node'
```

优先关闭页面重新打开、强制刷新或清理该站点缓存。这通常是前端状态异常，不代表同步失败。

最终状态以这些命令为准：

```bash
kubectl get application k8s-study -n argocd
kubectl get pods -n k8s-study
kubectl logs statefulset/argocd-application-controller -n argocd
```

## 12. 镜像仓库连接超时

```text
Failed to connect to asia-east1-docker.pkg.dev port 443
```

这是网络连通性问题，不是 Kubernetes YAML 问题。可以更换可访问镜像源、配置网络代理，或使用 `kind load docker-image`。

## 13. Helm `pending-install`

`pending-install` 表示 Helm 安装尚未完成，常见原因是镜像拉取失败或命令被中断。

```bash
helm status RELEASE_NAME -n NAMESPACE
kubectl get pods -n NAMESPACE
kubectl get events -n NAMESPACE --sort-by=.lastTimestamp
```

不要只看 Helm 状态，要继续检查 Pod 和 Events。

## 14. PVC 一直 `Pending`

本次 StorageClass 使用 `WaitForFirstConsumer`。没有 Pod 引用 PVC 时，`Pending` 是正常状态。

已有 Pod 引用但仍不绑定：

```bash
kubectl describe pvc storage-demo -n k8s-study
kubectl describe pod POD_NAME -n k8s-study
```

## 15. watch 只能指定一种资源

报错：

```text
error: you may only specify a single resource type
```

分别观察：

```bash
kubectl get pvc -n k8s-study --watch
kubectl get pods -n k8s-study --watch
```

## 16. zsh `no matches found`

zsh 把 `[0]` 当成了通配表达式：

```text
zsh: no matches found: custom-columns=...
```

给参数加单引号：

```bash
kubectl get pods -n k8s-study -l app=my-app \
  -o 'custom-columns=NAME:.metadata.name,IMAGE:.spec.containers[0].image'
```

## 17. 先判断“正常等待”还是“真正故障”

通常可以短暂等待：

- `ContainerCreating`
- 新节点短暂 `NotReady`
- 滚动更新中的 `Terminating`
- `WaitForFirstConsumer` PVC 的 `Pending`

需要立即查看 Events：

- `ImagePullBackOff`
- `CrashLoopBackOff`
- `CreateContainerConfigError`
- `FailedMount`
- Deployment 长时间 `0/N Available`

[上一篇：自建镜像与滚动发布](./07-自建镜像与滚动发布.md) | [下一篇：命令速查、清理与下一步](./09-命令速查清理与下一步.md)
