# 第 1 篇：学习地图与环境准备

## 本篇概述

- **为什么要学**：Kubernetes 不是一个单独命令，而是一组相互协作的资源和工具。先建立地图，后面看到 Pod、Helm、Argo CD 或 Rancher 时，才知道它们处在哪一层，而不是靠死记命令。
- **会学到什么**：常见资源类型、工具之间的分工、YAML 基本结构以及 kubectl context。
- **开始前需要**：已完成第 0 篇，本地 `study` 集群处于 `Ready`。
- **完成标志**：能说清 Deployment、Service、Helm、Argo CD 和 Rancher 分别负责什么。

> 尚未安装 Docker Desktop、kubectl、Kind 或 Helm，请先阅读[第 0 篇：从零安装本地 Kubernetes 环境](./00-从零安装本地Kubernetes环境.md)。

## 1. Kubernetes 一定需要多台服务器吗

生产集群通常有多台服务器，用于高可用和扩容；学习不需要购买服务器。

Kind 会在 Mac 的 Docker 中创建 Kubernetes 节点：

```text
Mac
└── Docker Desktop
    └── study-control-plane（Docker 容器）
        └── Kubernetes 控制平面 + 工作节点
```

单节点已经可以学习 Deployment、Service、Ingress、Helm、Argo CD 和 PVC。

## 2. 核心资源地图

| `kind` | 作用 | 常见场景 |
|---|---|---|
| `Deployment` | 管理长期运行的无状态 Pod | Web、API、微服务 |
| `StatefulSet` | 管理固定身份和独立存储 | MySQL、Kafka、Redis |
| `DaemonSet` | 每个节点运行一个 Pod | 日志、监控、网络代理 |
| `Job` | 执行一次后结束 | 数据迁移、批处理 |
| `CronJob` | 定时创建 Job | 备份、清理任务 |
| `Service` | 为 Pod 提供固定内部入口 | 服务发现、负载分发 |
| `Ingress` | 描述 HTTP/HTTPS 路由 | 域名、路径转发 |
| `ConfigMap` | 保存普通配置 | 配置文件、网页内容 |
| `Secret` | 保存敏感配置 | 密码、令牌、私钥 |
| `PVC` | 申请持久化存储 | 数据目录、上传文件 |

快速判断：

```text
持续运行且无状态 → Deployment
需要稳定身份/存储 → StatefulSet
运行一次就结束   → Job
按时间运行       → CronJob
每个节点都要运行 → DaemonSet
```

## 3. kubectl、Helm、Argo CD 和 Rancher 的区别

| 工具 | 主要作用 | 是否必须 |
|---|---|---|
| kubectl | 直接查看和操作 Kubernetes 资源 | 学习时必须掌握 |
| Helm | 安装和升级一整套 Kubernetes 应用 | 安装复杂组件时常用 |
| Argo CD | 持续把 Git 配置同步到集群 | GitOps 场景使用 |
| Rancher | 用图形界面管理一个或多个集群 | 不是学习前置条件 |

它们不是相互替代关系：

```text
kubectl  → 直接操作 Kubernetes API
Helm     → 把一套模板渲染并安装成 Kubernetes 资源
Argo CD  → 让 Git 中的配置持续成为集群期望状态
Rancher  → 在更高层提供集群和权限管理界面
```

入门时先学习 kubectl 和 YAML，再学习 Helm、Argo CD。Rancher 可以等需要管理多集群或团队权限时再使用。

## 4. YAML 的基本结构

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-app
  namespace: k8s-study
spec:
  replicas: 2
```

- `apiVersion`：使用哪个 Kubernetes API 版本。
- `kind`：创建什么资源。
- `metadata`：资源名称、命名空间和标签。
- `spec`：期望 Kubernetes 维持的状态。

Kubernetes 不根据文件名寻找资源。`pvc.yaml`、`storage.yaml` 都只是本地文件名；真正的资源身份是：

```text
资源类型 + 命名空间 + metadata.name
```

## 5. 检查本地工具

```bash
kubectl version --client
kind version
helm version
docker version
```

本次学习环境：

| 工具 | 版本 |
|---|---|
| kubectl | v1.36.3 |
| Kind | v0.32.0 |
| Helm | v4.2.3 |
| Docker | 29.6.2 |

这些命令只读取版本，不会修改系统或集群。

## 6. 确认 Kind 集群

```bash
kind get clusters
```

预期能看到：

```text
study
```

如果还没有 `study`，请回到第 0 篇执行创建步骤。

检查节点：

```bash
kubectl get nodes
```

刚创建时可能显示：

```text
study-control-plane   NotReady
```

稍等几十秒后应变成：

```text
study-control-plane   Ready
```

`NotReady` 表示节点组件仍在启动，`Ready` 才表示可以正常调度 Pod。

## 7. 确认 kubectl 连接的集群

```bash
kubectl config current-context
```

本次应输出：

```text
kind-study
```

`kubectl` 的所有操作都会发送到当前 context 对应的集群。

## 8. 本篇完成标准

- Docker Desktop 正常运行。
- `kubectl`、Kind、Helm 均可执行。
- `study-control-plane` 为 `Ready`。
- 当前 context 是 `kind-study`。

[上一篇：从零安装本地 Kubernetes 环境](./00-从零安装本地Kubernetes环境.md) | [下一篇：Deployment、Pod 与 Service](./02-Deployment-Pod与Service.md)
