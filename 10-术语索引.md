# 第 10 篇：Kubernetes 术语索引

## 本篇概述

- **为什么要学**：Kubernetes 的难点之一是术语多，而且很多名词处于不同层级。建立索引后，遇到陌生词可以先查白话含义，再回到对应篇章深入理解。
- **包含什么**：集群、YAML、工作负载、网络、配置、存储、发布工具、GitOps、状态与排错术语。
- **如何使用**：在本文件中使用 `Command+F` 搜索英文或中文术语。
- **完成标志**：能区分 Pod 与 Deployment、Service 与 Ingress、PVC 与 PV、Helm 与 Argo CD。

---

## 1. 最重要的关系图

### 应用运行链路

```text
Deployment
  ↓ 管理
ReplicaSet
  ↓ 创建
Pod
  ↓ 运行
Container
  ↓ 来源
Image
```

### 网络访问链路

```text
浏览器
  ↓
Ingress Controller（Traefik）
  ↓ 读取 Ingress 规则
Service
  ↓ 根据 Label 选择
Pod
```

### 持久化链路

```text
Container mountPath
  ↓
Pod Volume
  ↓ claimName
PVC
  ↓ 绑定
PV
  ↓ 由 StorageClass 提供
实际存储
```

### GitOps 链路

```text
本地 YAML
  ↓ git push
GitHub
  ↓ Argo CD 拉取
Application
  ↓ 同步
Kubernetes 集群
```

---

## 2. 集群与基础环境

| 术语 | 白话解释 | 详细篇章 |
|---|---|---|
| Kubernetes / K8s | 管理容器化应用的平台，负责部署、扩缩容、自愈、网络和配置。 | [第 1 篇](./01-学习地图与环境准备.md) |
| Cluster（集群） | 一组共同运行 Kubernetes 的节点和控制组件。本教程的集群名是 `study`。 | [第 0 篇](./00-从零安装本地Kubernetes环境.md) |
| Node（节点） | 实际运行 Pod 的机器。可以是服务器、虚拟机，也可以是 Kind 创建的 Docker 容器。 | [第 0 篇](./00-从零安装本地Kubernetes环境.md) |
| Control Plane（控制平面） | Kubernetes 的“大脑”，负责 API、调度和控制器协调。 | [第 0 篇](./00-从零安装本地Kubernetes环境.md) |
| API Server | Kubernetes 的统一入口。kubectl、Helm 和 Argo CD 最终都通过它读写资源。 | [第 1 篇](./01-学习地图与环境准备.md) |
| Kind | Kubernetes in Docker，在 Docker 容器中创建本地 Kubernetes 集群。 | [第 0 篇](./00-从零安装本地Kubernetes环境.md) |
| Docker Desktop | Mac 上的容器运行环境，为 Kind 节点和本地镜像构建提供基础。 | [第 0 篇](./00-从零安装本地Kubernetes环境.md) |
| containerd | Kind 节点内部实际负责运行容器、保存节点镜像的容器运行时。 | [第 7 篇](./07-自建镜像与滚动发布.md) |
| kubeconfig | 保存集群地址、用户凭证和 context 的 kubectl 配置文件。 | [第 1 篇](./01-学习地图与环境准备.md) |
| Context（上下文） | kubectl 当前使用的“集群 + 用户 + 默认命名空间”组合。本教程使用 `kind-study`。 | [第 1 篇](./01-学习地图与环境准备.md) |
| Namespace（命名空间） | 对资源进行逻辑分组和隔离。本教程的业务资源主要位于 `k8s-study`。 | [第 3 篇](./03-YAML配置与资源治理.md) |

---

## 3. YAML 与资源身份

| 术语 | 白话解释 | 示例 |
|---|---|---|
| Manifest（清单） | 描述 Kubernetes 资源的 YAML 或 JSON 文件。 | `web.yaml` |
| `apiVersion` | 这类资源使用的 Kubernetes API 版本。 | `apps/v1` |
| `kind` | 要创建的资源类型。 | `Deployment` |
| `metadata` | 资源身份和附加信息，包括名称、命名空间、Label 和 Annotation。 | `metadata.name` |
| `spec` | 使用者声明的期望状态。 | `replicas: 2` |
| `status` | Kubernetes 控制器记录的实际状态，通常不由使用者直接填写。 | `availableReplicas` |
| Name（名称） | 资源在所属范围内的名字。资源通过名字关联，不通过 YAML 文件名关联。 | `storage-demo` |
| Label（标签） | 附加在资源上的键值对，用于分类和选择。 | `app: web` |
| Selector（选择器） | 根据 Label 找到目标资源。Deployment 和 Service 都会使用。 | `app=web` |
| Annotation（注解） | 保存工具或控制器使用的附加信息，通常不用于选择资源。 | Argo CD refresh 注解 |
| CRD | CustomResourceDefinition，自定义资源定义。它让 Kubernetes 支持 Application 等扩展资源类型。 | Argo CD `Application` |

资源的真实身份通常是：

```text
API 类型 + Namespace + metadata.name
```

`pvc.yaml` 只是本地文件名。Pod 通过 `claimName: storage-demo` 查找 PVC，而不是查找名为 `pvc.yaml` 的文件。

---

## 4. 容器与工作负载

| 术语 | 白话解释 | 详细篇章 |
|---|---|---|
| Container（容器） | 镜像运行后的进程和隔离环境。 | [第 7 篇](./07-自建镜像与滚动发布.md) |
| Image（镜像） | 容器的只读运行模板，包含程序和依赖。 | [第 7 篇](./07-自建镜像与滚动发布.md) |
| Image Tag（镜像标签） | 标识镜像版本，例如 `v1`、`v2`。 | [第 7 篇](./07-自建镜像与滚动发布.md) |
| Registry（镜像仓库） | 存放和分发镜像的服务，例如 Docker Hub、GHCR。 | [第 7 篇](./07-自建镜像与滚动发布.md) |
| Dockerfile | 描述如何把代码和依赖构建成镜像的文件。 | [第 7 篇](./07-自建镜像与滚动发布.md) |
| Pod | Kubernetes 调度和运行容器的最小单位。一个 Pod 可包含一个或多个容器。 | [第 2 篇](./02-Deployment-Pod与Service.md) |
| Deployment | 管理无状态 Pod 的控制器，负责副本、自愈、滚动更新和回滚。 | [第 2 篇](./02-Deployment-Pod与Service.md) |
| ReplicaSet | Deployment 下层的副本控制器，负责维持指定数量的 Pod。通常不直接操作。 | [第 2 篇](./02-Deployment-Pod与Service.md) |
| Replica / replicas | Pod 副本数量。`replicas: 2` 表示希望维持两个实例。 | [第 2 篇](./02-Deployment-Pod与Service.md) |
| Rolling Update（滚动更新） | 逐步创建新 Pod、删除旧 Pod，尽量保持服务持续可用。 | [第 7 篇](./07-自建镜像与滚动发布.md) |
| Revision | Deployment 或 Helm Release 的历史版本编号，用于查看历史和回滚。 | [第 2 篇](./02-Deployment-Pod与Service.md) |
| StatefulSet | 管理需要稳定名称、顺序和独立存储的有状态 Pod。 | [第 9 篇](./09-命令速查清理与下一步.md) |
| DaemonSet | 保证每个符合条件的节点运行一个 Pod，常用于日志和监控代理。 | [第 1 篇](./01-学习地图与环境准备.md) |
| Job | 执行一次任务，成功完成后退出。 | [第 1 篇](./01-学习地图与环境准备.md) |
| CronJob | 按时间计划定期创建 Job。 | [第 1 篇](./01-学习地图与环境准备.md) |

---

## 5. Service 与网络

| 术语 | 白话解释 | 详细篇章 |
|---|---|---|
| Pod IP | 分配给单个 Pod 的 IP。Pod 重建后可能变化。 | [第 2 篇](./02-Deployment-Pod与Service.md) |
| Service | 为一组 Pod 提供固定访问入口和内部负载分发。 | [第 2 篇](./02-Deployment-Pod与Service.md) |
| ClusterIP | 只能从集群内部访问的 Service 类型，也是默认类型。 | [第 2 篇](./02-Deployment-Pod与Service.md) |
| Service IP | Kubernetes 为 ClusterIP Service 分配的虚拟 IP，不属于某个具体 Pod。 | [第 2 篇](./02-Deployment-Pod与Service.md) |
| EndpointSlice | 记录 Service 当前可以转发到哪些 Pod IP 和端口。 | [第 2 篇](./02-Deployment-Pod与Service.md) |
| Service DNS | 集群内部通过 Service 名访问服务，例如 `http://web-service`。 | [第 2 篇](./02-Deployment-Pod与Service.md) |
| `port` | Service 对访问者提供的端口。 | [第 3 篇](./03-YAML配置与资源治理.md) |
| `targetPort` | Service 转发到后端容器的端口。 | [第 3 篇](./03-YAML配置与资源治理.md) |
| port-forward | 在本机和集群资源之间建立临时端口隧道。终端结束后隧道消失。 | [第 2 篇](./02-Deployment-Pod与Service.md) |
| Ingress | 描述 HTTP/HTTPS 域名和路径应该转发到哪个 Service。 | [第 4 篇](./04-Helm-Traefik与Ingress.md) |
| Ingress Controller | 真正读取 Ingress 规则并处理流量的程序。 | [第 4 篇](./04-Helm-Traefik与Ingress.md) |
| IngressClass | 指定某条 Ingress 规则由哪个 Controller 处理。 | [第 4 篇](./04-Helm-Traefik与Ingress.md) |
| Traefik | 一种 Ingress Controller 实现，本教程使用它处理 HTTP 路由。 | [第 4 篇](./04-Helm-Traefik与Ingress.md) |
| ingress-nginx | 另一种基于 Nginx 的 Ingress Controller，与 Traefik 二选一即可。 | [第 4 篇](./04-Helm-Traefik与Ingress.md) |
| Load Balancing（负载分发） | 把不同连接分发给多个可用 Pod。 | [第 2 篇](./02-Deployment-Pod与Service.md) |

---

## 6. 配置、健康与资源

| 术语 | 白话解释 | 详细篇章 |
|---|---|---|
| ConfigMap | 保存非敏感配置，可以作为文件或环境变量提供给 Pod。 | [第 3 篇](./03-YAML配置与资源治理.md) |
| Secret | 保存密码、令牌、证书和私钥等敏感配置。base64 不等于加密。 | [第 3 篇](./03-YAML配置与资源治理.md) |
| Environment Variable | 以环境变量形式把配置传入容器。 | [第 3 篇](./03-YAML配置与资源治理.md) |
| Volume | Pod 声明的一块可挂载数据来源，可以来自 ConfigMap、Secret 或 PVC。 | [第 3 篇](./03-YAML配置与资源治理.md) |
| `volumeMounts` | 指定容器把某个 Pod Volume 挂到哪个目录。 | [第 6 篇](./06-PVC-PV与持久化存储.md) |
| `mountPath` | 容器内部看到的挂载路径。它不写在 PVC 中。 | [第 6 篇](./06-PVC-PV与持久化存储.md) |
| readinessProbe | 判断 Pod 是否准备好接收流量。失败时 Service 暂停向它转发。 | [第 3 篇](./03-YAML配置与资源治理.md) |
| livenessProbe | 判断容器是否仍然存活。持续失败时 Kubernetes 会重启容器。 | [第 3 篇](./03-YAML配置与资源治理.md) |
| requests | 容器调度时声明的最低 CPU 和内存需求。 | [第 3 篇](./03-YAML配置与资源治理.md) |
| limits | 容器允许使用的 CPU 和内存上限。 | [第 3 篇](./03-YAML配置与资源治理.md) |
| `m` CPU | CPU 的千分之一单位。`50m` 表示 0.05 个 CPU 核心。 | [第 3 篇](./03-YAML配置与资源治理.md) |
| `Mi` | 二进制内存单位 MiB，例如 `128Mi`。 | [第 3 篇](./03-YAML配置与资源治理.md) |

---

## 7. 持久化存储

| 术语 | 白话解释 | 详细篇章 |
|---|---|---|
| StorageClass | 定义 Kubernetes 应该怎样自动供应存储。 | [第 6 篇](./06-PVC-PV与持久化存储.md) |
| PVC | PersistentVolumeClaim，应用提出的存储申请，例如申请 100Mi。 | [第 6 篇](./06-PVC-PV与持久化存储.md) |
| PV | PersistentVolume，满足 PVC 申请的实际存储资源，属于集群级资源。 | [第 6 篇](./06-PVC-PV与持久化存储.md) |
| Bound | PVC 已成功绑定到 PV。 | [第 6 篇](./06-PVC-PV与持久化存储.md) |
| Pending | PVC 尚未绑定。使用 `WaitForFirstConsumer` 时，没有 Pod 引用前可能是正常状态。 | [第 6 篇](./06-PVC-PV与持久化存储.md) |
| RWO | ReadWriteOnce，可由一个节点以读写方式挂载。它不严格等于“只能有一个 Pod”。 | [第 6 篇](./06-PVC-PV与持久化存储.md) |
| ReclaimPolicy | PV 释放后的回收策略。`Delete` 表示删除 PVC 后底层存储也会被回收。 | [第 6 篇](./06-PVC-PV与持久化存储.md) |
| WaitForFirstConsumer | 等真正使用 PVC 的 Pod 出现后，再决定在哪个节点创建存储。 | [第 6 篇](./06-PVC-PV与持久化存储.md) |

---

## 8. kubectl、Helm 与 Rancher

| 术语 | 白话解释 | 详细篇章 |
|---|---|---|
| kubectl | Kubernetes 官方命令行客户端，直接读写 Kubernetes API。 | [第 1 篇](./01-学习地图与环境准备.md) |
| Helm | Kubernetes 包管理器，用于安装、升级和回滚一整套应用。 | [第 4 篇](./04-Helm-Traefik与Ingress.md) |
| Chart | Helm 应用包，内部包含 YAML 模板和默认参数。 | [第 4 篇](./04-Helm-Traefik与Ingress.md) |
| Release | 某个 Chart 在集群中的一次安装实例。 | [第 4 篇](./04-Helm-Traefik与Ingress.md) |
| Values | Helm Chart 的参数，用来改变镜像、副本数和 Service 类型等配置。 | [第 4 篇](./04-Helm-Traefik与Ingress.md) |
| Helm Repository | 存放和分发 Chart 的仓库，不是 Docker 镜像仓库。 | [第 4 篇](./04-Helm-Traefik与Ingress.md) |
| Rancher | 在更高层提供集群、权限和工作负载管理界面的平台，不是 Kubernetes 的必需组件。 | [第 1 篇](./01-学习地图与环境准备.md) |

---

## 9. GitOps 与 Argo CD

| 术语 | 白话解释 | 详细篇章 |
|---|---|---|
| GitOps | 用 Git 保存期望状态，由控制器自动让集群与 Git 保持一致。 | [第 5 篇](./05-ArgoCD与GitOps.md) |
| Argo CD | 实现 Kubernetes GitOps 的控制器和管理界面。 | [第 5 篇](./05-ArgoCD与GitOps.md) |
| Application | Argo CD 自定义资源，描述仓库、分支、目录和部署目标。 | [第 5 篇](./05-ArgoCD与GitOps.md) |
| Repository Secret | 保存 Argo CD 访问私有仓库所需的地址和凭证。 | [第 5 篇](./05-ArgoCD与GitOps.md) |
| Deploy Key | 只授权访问某个 Git 仓库的 SSH Key。Argo CD 通常只需要读取权限。 | [第 5 篇](./05-ArgoCD与GitOps.md) |
| Synced | Argo CD 当前读取的 Git 配置与集群一致。仍需核对它读取的提交是否最新。 | [第 5 篇](./05-ArgoCD与GitOps.md) |
| OutOfSync | Git 期望状态与集群实际状态存在差异。 | [第 5 篇](./05-ArgoCD与GitOps.md) |
| Healthy | Argo CD 判断应用资源已经健康。 | [第 5 篇](./05-ArgoCD与GitOps.md) |
| Progressing | 资源仍在启动，或健康状态尚未完成汇总。 | [第 5 篇](./05-ArgoCD与GitOps.md) |
| selfHeal | 发现集群被手动改动后，自动恢复 Git 中的配置。 | [第 5 篇](./05-ArgoCD与GitOps.md) |
| prune | Git 删除资源后，是否允许 Argo CD 删除集群中的旧资源。 | [第 5 篇](./05-ArgoCD与GitOps.md) |
| Webhook | GitHub 在 push 后主动通知 Argo CD。没有 Webhook 时通常依赖周期轮询。 | [第 5 篇](./05-ArgoCD与GitOps.md) |
| Git Revision | Git 提交哈希，代表一份确定的仓库状态。 | [第 5 篇](./05-ArgoCD与GitOps.md) |

---

## 10. 常见状态与错误

| 状态或错误 | 白话解释 | 首先检查 |
|---|---|---|
| `Ready` | 节点或容器已达到可用条件。 | `kubectl get nodes/pods` |
| `NotReady` | 节点尚未可用，刚创建集群时可能短暂出现。 | `kubectl describe node` |
| `ContainerCreating` | Pod 已调度，正在创建容器、挂载或准备网络。 | 等待；长时间不变再看 Events |
| `Running` | Pod 已运行，但仍需查看 `READY` 判断容器是否就绪。 | `kubectl get pods` |
| `Completed` | Job 等一次性任务已经成功执行并退出，不是故障。 | `kubectl logs` |
| `Terminating` | 资源正在删除，滚动更新时经常出现。 | 等待或查看 finalizer |
| `ImagePullBackOff` | Kubernetes 多次拉取镜像失败，正在延迟重试。 | Pod Events、镜像名和网络 |
| `CrashLoopBackOff` | 容器反复启动后退出，Kubernetes 正在延迟重启。 | 容器日志和退出码 |
| `OOMKilled` | 容器使用内存超过限制，被系统终止。 | limits 和实际内存需求 |
| `FailedMount` | Pod 无法挂载 ConfigMap、Secret 或 PVC。 | 名称、Namespace、PVC 状态 |
| `NotFound` | 当前集群或命名空间中找不到指定资源。 | context、namespace、资源名 |
| `AlreadyExists` | 同名资源已存在。重复执行创建命令时可能正常。 | 先执行 `kubectl get` |

完整排查步骤见[第 8 篇](./08-常见故障排查手册.md)。

---

## 11. 尚未深入但以后会遇到的术语

| 术语 | 白话解释 |
|---|---|
| HPA | HorizontalPodAutoscaler，根据 CPU、内存或自定义指标自动调整 Pod 副本数。 |
| RBAC | Role-Based Access Control，控制用户和服务账号可以操作哪些 Kubernetes 资源。 |
| ServiceAccount | Pod 在集群内部访问 Kubernetes API 时使用的身份。 |
| NetworkPolicy | 限制哪些 Pod 或网络地址可以相互通信。 |
| Operator | 使用自定义资源和控制器管理复杂有状态应用的软件。 |
| Prometheus | 收集和查询指标的监控系统。 |
| Grafana | 展示指标和构建监控面板的工具。 |
| TLS | 对 HTTPS 通信进行加密和身份验证。 |
| Ingress TLS | 为 Ingress 域名配置 HTTPS 证书。 |
| CI | 持续集成，自动测试并构建镜像。 |
| CD | 持续交付或持续部署，把已验证版本发布到环境。Argo CD 属于 CD/GitOps 工具。 |

这些内容不属于当前入门主线。需要真实项目时，再按照[第 9 篇](./09-命令速查清理与下一步.md)的路线继续学习。

---

## 12. 四组最容易混淆的术语

### Pod 与 Deployment

```text
Pod        → 实际运行容器的实例，会被替换
Deployment → 管理 Pod，维持副本并滚动更新
```

### Service 与 Ingress

```text
Service → 为 Pod 提供稳定的集群内部入口
Ingress → 描述 HTTP 域名和路径如何进入 Service
```

### PVC 与 PV

```text
PVC → 应用提出的存储申请
PV  → 满足申请的实际存储资源
```

### Helm 与 Argo CD

```text
Helm    → 渲染、安装和升级一套应用包
Argo CD → 持续比较 Git 与集群并自动同步
```

[上一篇：命令速查、清理与下一步](./09-命令速查清理与下一步.md) | [返回目录](./README.md)

